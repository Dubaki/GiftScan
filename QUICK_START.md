# ⚡ GiftScan - Быстрый старт за 5 минут

## 🎯 Для тестирования локально

### Шаг 1: Запуск инфраструктуры (30 сек)
```bash
# Запустить PostgreSQL и Redis
docker-compose up -d

# Проверить что запустились
docker ps
```

### Шаг 2: Инициализация БД (10 сек, только первый раз)
```bash
cd backend
python -m alembic upgrade head
cd ..
```

### Шаг 3: Запуск приложения (1 мин)

**Windows - автоматически:**
```bash
start_dev.bat
```

**Или вручную в 3 окнах терминала:**

Окно 1 - Backend:
```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Окно 2 - Frontend:
```bash
cd frontend
npm install  # только первый раз
npm run dev
```

Окно 3 - Ngrok (для Telegram):
```bash
ngrok http 5173
```

### Шаг 4: Тестирование (2 мин)

Проверьте что всё работает:

```bash
# Backend API
curl http://localhost:8000/health

# Frontend
curl http://localhost:5173

# API через frontend proxy
curl http://localhost:5173/api/v1/gifts?limit=1
```

Откройте в браузере:
- Backend docs: http://localhost:8000/docs
- Frontend app: http://localhost:5173

---

## 📱 Для Telegram Mini App

### Шаг 1: Создать бота (2 мин)
1. Открыть [@BotFather](https://t.me/BotFather)
2. `/newbot` → Ввести имя и username
3. Сохранить **токен бота**

### Шаг 2: Создать Mini App (2 мин)
1. У @BotFather: `/newapp`
2. Выбрать своего бота
3. Title: `GiftScan`
4. Description: `TON NFT Gift Price Scanner`
5. Skip photo и demo
6. **Web App URL**: скопировать из ngrok (например: `https://abc123.ngrok.io`)
7. Short name: `giftscan`

### Шаг 3: Открыть в Telegram
```
https://t.me/ваш_бот/giftscan
```

Или просто:
1. Открыть своего бота в Telegram
2. Нажать кнопку "Open App"

---

## 🎁 Первое использование

### В Telegram Mini App вы увидите:

1. **Список подарков** - все 35+ типов
2. **Цены** - с Fragment, GetGems, MRKT, Portals, Tonnel
3. **Арбитраж** - badge если спред > 5%
4. **Поиск** - найти конкретный подарок
5. **Детали** - клик на подарок → таблица цен

### Пример:
```
🎁 Bling Binky                      +351%
Frag:33 GG:35 MR:149 Ton:58
33.00 TON  ← Fragment
```

Клик → откроется modal с деталями:
- Best price: 33 TON (Fragment)
- Worst price: 149 TON (MRKT)
- **Spread: 116 TON (351%) ← АРБИТРАЖ!**

---

## 🔧 Решение проблем

### Backend не запускается
```bash
# Проверить что БД работает
docker ps | grep giftscan

# Если нет - запустить
docker-compose up -d
```

### Frontend не грузит данные
```bash
# Проверить что backend работает
curl http://localhost:8000/health

# Проверить proxy в vite.config.js
cd frontend
cat vite.config.js
```

### Telegram Mini App не открывается
1. URL должен быть **HTTPS** (используйте ngrok)
2. Проверьте что ngrok работает: `curl https://ваш-url.ngrok.io`
3. Попробуйте обновить URL в @BotFather: `/myapps`

### Цены старые
```bash
# Запустить ручное сканирование
cd backend
python run_single_scan.py
```

---

## 📊 Полезные команды

### Проверить каталог подарков
```bash
cd backend
python check_catalog.py
```

### Посмотреть последние цены
```bash
cd backend
python check_recent_prices.py
```

### Запустить одно сканирование
```bash
cd backend
python run_single_scan.py
```

### Добавить новые подарки
```bash
cd backend
python discover_new_gifts.py
```

---

## 🎓 Следующие шаги

1. ✅ Протестировали локально
2. ✅ Создали Telegram бота
3. ✅ Запустили Mini App
4. 📖 Прочитать **FEATURES.md** - список всех возможностей
5. 📖 Прочитать **TELEGRAM_SETUP.md** - детальная настройка
6. 🚀 Deploy на production (Vercel + Railway)

---

## ⏱️ Итого:
- **Локальный запуск**: 5 минут
- **Telegram Mini App**: +4 минуты
- **Всего**: ~10 минут до полностью рабочего приложения!

**Приятного использования! 🎁🚀**
