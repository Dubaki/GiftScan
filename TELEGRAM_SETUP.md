# 📱 Настройка Telegram Mini App для GiftScan

## Шаг 1: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `GiftScan`)
4. Введите username бота (например: `giftscan_bot`)
5. Сохраните **токен бота** - он понадобится для backend

## Шаг 2: Настройка Mini App

1. В чате с @BotFather отправьте `/newapp`
2. Выберите своего бота из списка
3. **Title**: `GiftScan`
4. **Description**: `TON NFT Gift Price Scanner - Compare prices across all marketplaces`
5. **Photo**: Загрузите иконку бота (640x360px, можно пропустить)
6. **Demo GIF**: Пропустите (отправьте `/empty`)
7. **Web App URL**:
   - Для разработки: `https://ваш-туннель.ngrok.io` (см. Шаг 3)
   - Для продакшена: `https://ваш-домен.com`
8. **Short name**: `giftscan` (используется в ссылке)

## Шаг 3: Настройка туннеля для разработки (ngrok)

### Вариант A: ngrok (рекомендуется)

1. Установите ngrok: https://ngrok.com/download
2. Запустите туннель:
   ```bash
   ngrok http 5173
   ```
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
4. Вернитесь к Шагу 2 и вставьте этот URL

### Вариант B: localtunnel

```bash
npx localtunnel --port 5173
```

## Шаг 4: Обновление backend .env

Добавьте токен бота в `backend/.env`:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id  # для уведомлений (опционально)
```

## Шаг 5: Запуск приложения

### Backend:
```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Frontend:
```bash
cd frontend
npm run dev
```

### Ngrok (в отдельном терминале):
```bash
ngrok http 5173
```

## Шаг 6: Тестирование

1. Откройте свой бот в Telegram
2. Отправьте команду `/start`
3. Нажмите на кнопку **"Open App"** или используйте ссылку:
   ```
   https://t.me/ваш_бот/giftscan
   ```

## 🎯 Что увидит пользователь:

1. **Главный экран** - список всех подарков с ценами:
   - Thumbnail подарка
   - Название
   - Лучшая цена (зеленым)
   - Все цены с площадок (мелким текстом)
   - Спред арбитража (если есть)

2. **Детали подарка** (при клике):
   - Полная информация
   - Best Price / Worst Price
   - Spread (разница)
   - Таблица цен со всех площадок
   - Ссылки для покупки

3. **Поиск и фильтры**:
   - Поиск по названию
   - Сортировка (по имени, цене, спреду)
   - Фильтр по минимальному спреду

## 🚀 Деплой на продакшен

### Вариант 1: Vercel (для frontend)

```bash
cd frontend
npm install -g vercel
vercel
```

Установите переменную окружения:
- `VITE_API_URL` = URL вашего backend API

### Вариант 2: Railway (для backend)

1. Зарегистрируйтесь на https://railway.app
2. Подключите GitHub репозиторий
3. Укажите root directory: `backend`
4. Добавьте переменные окружения из `.env`

### Обновление Web App URL

После деплоя обновите URL в @BotFather:
```
/setmenubutton
Выбрать бота
Отправить новый URL продакшена
```

## 📝 Дополнительные команды для бота

Вы можете добавить команды в @BotFather:

```
/mycommands
start - Запустить сканер
help - Помощь
prices - Актуальные цены
```

## 🔧 Troubleshooting

**Проблема**: "Bot domain invalid"
- **Решение**: URL должен быть HTTPS

**Проблема**: "App не загружается"
- Проверьте что backend работает
- Проверьте CORS настройки в backend
- Проверьте что ngrok туннель активен

**Проблема**: "Цены не загружаются"
- Проверьте `VITE_API_URL` в frontend/.env
- Проверьте proxy в vite.config.js
- Проверьте что backend API доступен

## 💡 Полезные ссылки

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [TWA SDK GitHub](https://github.com/twa-dev/sdk)
- [ngrok Documentation](https://ngrok.com/docs)
