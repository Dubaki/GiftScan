Задача: Переписать логику сбора данных, сделав TonAPI основным и самым стабильным источником данных о ценах на NFT-подарки. Уйти от использования нестабильных tma токенов там, где это возможно.

1. Настройка TonAPI Collector:

Реализуй класс TonApiCollector. Используй эндпоинт https://tonapi.io/v2/nfts/collections/{address}/items.

Адрес коллекции для мониторинга: EQCA14o1-VzcG9ogm8S_6YvN0I-f-6rYqYqYqYqYqYqYqYqY (основная коллекция подарков Telegram).

Бот должен получать список NFT, которые имеют статус "sale" (выставлены на продажу).

Используй TONAPI_KEY из .env для авторизации в заголовках: {"Authorization": "Bearer " + TONAPI_KEY}.

2. Логика агрегации цен (Multi-Marketplace):

Поскольку TonAPI видит лоты с разных площадок (GetGems, Portals, MRKT), научи бота парсить поле marketplace или адрес контракта магазина в данных предмета.

Реализуй нормализацию: из метаданных NFT извлекай название подарка (например, "Milk Coffee") и его номер (атрибут number или часть имени).

3. Сравнение с Fragment (Benchmark):

Оставь модуль парсинга Fragment.com (через BeautifulSoup), так как это «эталонная» площадка с самыми высокими ценами.

Бот должен сравнивать самую низкую цену (Floor), найденную через TonAPI (на любой площадке), с ценой аналогичного подарка на Fragment.

4. Арбитражный калькулятор 2.0:

Входные данные: Price_Lowest_NFT (из TonAPI) и Price_Fragment (со скрапера).

Формула профита: Profit = Price_Fragment - Price_Lowest_NFT - (Market_Fee + Gas).

Market_Fee рассчитывай как 5% от цены продажи.

Gas фиксируй как 0.08 TON.

5. Система уведомлений:

При обнаружении профита выше MIN_PROFIT_TON (из .env), бот должен отправить сообщение в Telegram:

🏷 Тип: {Gift Name} #{Number}

💰 Цена покупки: {Price} TON (нашел через TonAPI)

📈 Цена на Fragment: {Price} TON

💸 Чистый профит: {Profit} TON

🔗 Ссылка на покупку: (формируй ссылку на GetGems или Portals по адресу NFT).

Технические требования:

Используй библиотеку pytoniq или просто aiohttp для запросов к TonAPI.

Соблюдай Rate Limits бесплатного тарифа TonAPI (1 запрос в секунду).

Сделай код модульным: один файл для сбора данных, один для логики, один для уведомлений.